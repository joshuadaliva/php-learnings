<?php
	
	// string methods

	$username = 'joshua daliva';
	$phone = "123-456-789";
	$username1 = ["joshua", "daliva"];

	// $username = strtolower($username); //convert to lower case
	// $username = strtoupper($username); //conver to upper case
	// $username = trim($username); //removes the white space around
	// $username = str_pad($username, "20", "*"); //adds a symbol
	// $phone = str_replace("-", "" ,$phone); //replace a symbol or text
	// $username = strrev($username); //reverse a string
	// $username = str_shuffle($username); //shuffle a string
	// $equals = strcmp($username, "dsa"); //compares a string
	// $len = strlen($username); // length of a string
	// $fullname = explode(" ", $username); //divides the string and then make it new array
	// $fullname = implode("=", $username1); converts an array to a string

	// echo "$username";
	// echo "$phone";
	// echo "$equals";
	// echo "$len";
	// foreach($fullname as $name){
	// 	echo "$name" . " ";
	// }
	// echo "$fullname";

?>